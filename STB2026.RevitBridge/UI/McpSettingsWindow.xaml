<Window x:Class="STB2026.RevitBridge.UI.McpSettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="STB2026 — A.I. Connector for Revit"
        Width="480" Height="440"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White"
        FontFamily="Segoe UI"
        FontSize="13">

    <Window.Resources>
        <!-- Toggle Switch стиль -->
        <Style x:Key="ToggleSwitch" TargetType="ToggleButton">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Grid>
                            <Border x:Name="bg" Width="44" Height="22" CornerRadius="11"
                                    Background="#CCC" HorizontalAlignment="Left">
                                <Ellipse x:Name="dot" Width="18" Height="18" Fill="White"
                                         HorizontalAlignment="Left" Margin="2,0,0,0"/>
                            </Border>
                            <TextBlock Text="{TemplateBinding Content}" Margin="52,0,0,0"
                                       VerticalAlignment="Center"/>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="bg" Property="Background" Value="#5B5FC7"/>
                                <Setter TargetName="dot" Property="HorizontalAlignment" Value="Right"/>
                                <Setter TargetName="dot" Property="Margin" Value="0,0,2,0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Статус иконки -->
        <Style x:Key="StatusOk" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2E7D32"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="StatusFail" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#C62828"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style x:Key="StatusWarn" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#E65100"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="A.I. Connector for Revit" FontSize="18" FontWeight="SemiBold"/>
        <TextBlock Grid.Row="1" Text="🔗 Connects Claude Desktop App or Cursor to Revit."
                   Foreground="#666" Margin="0,2,0,8"/>

        <!-- Табы Auto / Manual -->
        <TabControl x:Name="tabMode" Grid.Row="2" Background="White" BorderThickness="0" Margin="0,4,0,8">

            <!-- TAB: Auto -->
            <TabItem Header="     Auto     " FontSize="14" FontWeight="SemiBold">
                <StackPanel Margin="4,12,4,0">
                    <!-- Claude Desktop -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock x:Name="txtClaudeIcon" Style="{StaticResource StatusFail}" Text="✗ " Width="22"/>
                        <TextBlock x:Name="txtClaudeStatus" Text="Claude Desktop App не обнаружен."
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="btnSetupClaude" Content="  Подключить Claude  " Padding="8,4"
                            Margin="22,0,0,10" HorizontalAlignment="Left"
                            Click="BtnSetupClaude_Click" Visibility="Collapsed"/>

                    <!-- Cursor -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock x:Name="txtCursorIcon" Style="{StaticResource StatusFail}" Text="✗ " Width="22"/>
                        <TextBlock x:Name="txtCursorStatus" Text="Cursor не обнаружен."
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button x:Name="btnSetupCursor" Content="  Подключить Cursor  " Padding="8,4"
                            Margin="22,0,0,10" HorizontalAlignment="Left"
                            Click="BtnSetupCursor_Click" Visibility="Collapsed"/>

                    <!-- Claude Code -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock x:Name="txtCodeIcon" Style="{StaticResource StatusFail}" Text="✗ " Width="22"/>
                        <TextBlock x:Name="txtCodeStatus" Text="Claude Code не обнаружен."
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Pipe статус -->
                    <Border BorderBrush="#E0E0E0" BorderThickness="0,1,0,0" Margin="0,8,0,0" Padding="0,8,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock x:Name="txtPipeIcon" Text="⏳" FontSize="14" Width="22"/>
                            <TextBlock x:Name="txtPipeStatus" Text="Named Pipe: ожидание запуска..."
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                    <!-- Restart banner -->
                    <Border x:Name="pnlRestart" Background="#FFF3E0" CornerRadius="4"
                            Padding="10,6" Margin="0,10,0,0" Visibility="Collapsed">
                        <TextBlock Text="⚠ Перезапустите AI-приложение для применения настроек."
                                   Foreground="#E65100" TextWrapping="Wrap"/>
                    </Border>
                </StackPanel>
            </TabItem>

            <!-- TAB: Manual -->
            <TabItem Header="     Manual     " FontSize="14" FontWeight="SemiBold">
                <StackPanel Margin="4,12,4,0">
                    <TextBlock Text="Путь к конфигу Claude:" Margin="0,0,0,4"/>
                    <TextBox x:Name="txtConfigPath" IsReadOnly="True" Background="#F5F5F5"
                             Padding="4" Margin="0,0,0,10"/>

                    <TextBlock Text="JSON для claude_desktop_config.json:" Margin="0,0,0,4"/>
                    <TextBox x:Name="txtManualJson" IsReadOnly="True" Background="#F5F5F5"
                             Height="120" AcceptsReturn="True" TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" Padding="4" FontFamily="Consolas" FontSize="11"/>

                    <Button Content="  📋 Copy JSON  " Padding="8,4"
                            Margin="0,8,0,0" HorizontalAlignment="Left"
                            Click="BtnCopyJson_Click"/>
                </StackPanel>
            </TabItem>
        </TabControl>

        <!-- Тоглы -->
        <StackPanel Grid.Row="3" Margin="0,4,0,8">
            <ToggleButton x:Name="tglConnection" Style="{StaticResource ToggleSwitch}"
                          Content="MCP Connection" Margin="0,0,0,6"
                          Checked="TglConnection_Changed" Unchecked="TglConnection_Changed"/>
            <ToggleButton x:Name="tglEditTools" Style="{StaticResource ToggleSwitch}"
                          Content="Allow Edit Tools (modify_model, create_mep)"
                          Checked="TglEditTools_Changed" Unchecked="TglEditTools_Changed"/>
        </StackPanel>

        <!-- Кнопка закрыть -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <TextBlock Text="STB2026 MCP Bridge" Foreground="#999" VerticalAlignment="Center" Margin="0,0,12,0"/>
            <Button Content="  Закрыть  " Padding="12,4" Click="BtnClose_Click"
                    Foreground="#5B5FC7" FontWeight="SemiBold" FontSize="11"
                    VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>
